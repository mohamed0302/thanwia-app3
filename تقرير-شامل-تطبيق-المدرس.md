# تقرير شامل — تطبيق المدرس (FinApp)

هذا التقرير يوثّق **هيكل التطبيق الحالي** وكل الملفات والميزات المنجزة في المشروع، بناءً على تحليل الكود الموجود.  
*(ملاحظة: المشروع غير مرتبط بـ Git حالياً، لذلك لا يوجد سجل commits لترتيب التغييرات زمنياً.)*

> **لتقرير أحدث وأشمل** يشمل التقنيات والـ API وبنية Firebase بالكامل، راجع: [docs/معلومات-التطبيق.md](docs/معلومات-التطبيق.md)

---

## المبرمج والمطوّر

| البند | القيمة |
|-------|--------|
| **الاسم** | محمد إيهاب قاسم (Mohamed Ehab Kassem) |
| **الدور** | مطوّر التطبيق بالكامل |
| **الهاتف** | +201017027347 |
| **البريد** | mohamed0kassem1@gmail.com |
| **LinkedIn** | [mohamed-kassem-3a37b9375](https://www.linkedin.com/in/mohamed-kassem-3a37b9375?utm_source=share&utm_campaign=share_via&utm_content=profile&utm_medium=android_app) |

---

## 1. نظرة عامة على التطبيق

**الاسم:** تطبيق المدرس — FinApp  
**الغرض:** تطبيق ويب/موبايل خفيف للمدرس لإدارة:
- **فيديوهات** الدروس والمحاضرات
- **حجوزات** الطلبة ومواعيد الحصص
- **الغياب والدرجات** ومتابعة الطلبة

**التقنيات الرئيسية:**
- **Frontend:** React 18 + Vite 7 + Tailwind CSS + React Router 6
- **Backend:** Node.js + Express + JWT + bcrypt
- **التخزين:** ملف `server/data.json` (قابل للاستبدال بقاعدة بيانات لاحقاً)
- **الموبايل:** إعدادات Capacitor جاهزة لبناء تطبيق أندرويد (APK)

---

## 2. هيكل المشروع (ما تم بناؤه)

```
finapp/
├── index.html              # نقطة الدخول HTML (RTL، لغة عربية)
├── package.json            # حزم الواجهة (React, Vite, Tailwind, React Router)
├── vite.config.js          # إعدادات Vite + proxy لـ /api → localhost:3000
├── tailwind.config.js      # ألوان primary، خطوط، ظلال
├── postcss.config.js       # PostCSS لـ Tailwind
├── capacitor.config.json   # إعدادات تطبيق أندرويد (com.finapp.teacher)
├── README.md               # تعليمات التشغيل والبناء
│
├── src/
│   ├── main.jsx            # تشغيل React على #root
│   ├── App.jsx             # التوجيه الرئيسي + AuthProvider + ProtectedRoute
│   ├── index.css           # Tailwind + .btn-primary, .card, .safe-bottom
│   │
│   ├── context/
│   │   └── AuthContext.jsx # تسجيل الدخول/الخروج، حفظ token و user في localStorage
│   │
│   ├── lib/
│   │   └── api.js          # عميل HTTP (get/post/put/delete) + دعم demo/guest بدون سيرفر
│   │
│   ├── components/
│   │   ├── Layout.jsx      # هيدر + محتوى + شريط سفلي (موبايل فقط)
│   │   ├── Header.jsx      # عنوان الصفحة + لوحة التحكم + خروج
│   │   ├── BottomNav.jsx   # تنقل: الرئيسية، فيديوهات، حجوزات، غياب ودرجات
│   │   ├── Card.jsx        # بطاقة عامة (قابلة للنقر أو للعرض فقط)
│   │   └── ProtectedRoute.jsx # إعادة توجيه لـ /login إذا لم يكن المستخدم مسجلاً
│   │
│   └── pages/
│       ├── Login.jsx       # تسجيل الدخول (بريد + كلمة مرور) + دخول زائر + تجريبي بدون سيرفر
│       ├── Home.jsx        # الصفحة الرئيسية + اختصارات للأقسام
│       ├── Videos.jsx      # قائمة الفيديوهات من API مع رابط "مشاهدة"
│       ├── Booking.jsx     # قائمة حجوزات الطلبة (اسم، تاريخ، وقت، حالة، ملاحظة)
│       ├── Grades.jsx      # قائمة الغياب والدرجات مع تمييز "انتباه" لغياب > 3
│       └── Dashboard.jsx   # لوحة تحكم: إحصائيات (فيديوهات، حجوزات، طلبة) + روابط
│
└── server/
    ├── package.json        # express, cors, jsonwebtoken, bcryptjs
    ├── index.js            # Backend: تسجيل دخول، فيديوهات، حجوزات، درجات، JWT، data.json
    └── data.json           # يُنشأ تلقائياً عند أول تشغيل (users, videos, bookings, grades)
```

---

## 3. ما تم تنفيذه بالتفصيل

### 3.1 الواجهة الأمامية (Frontend)

| العنصر | الوصف |
|--------|--------|
| **التوجيه (Routing)** | مسارات: `/login`, `/`, `/videos`, `/booking`, `/grades`, `/dashboard` مع عناوين عربية لكل صفحة. |
| **المصادقة (Auth)** | تسجيل دخول حقيقي عبر API، أو دخول زائر (بدون API)، أو وضع تجريبي عند عدم تشغيل السيرفر (أي بريد + كلمة المرور 123456). |
| **الحماية** | الصفحات الداخلية محمية بـ `ProtectedRoute`؛ غير المسجل يُحوَّل إلى `/login` مع حفظ `redirect`. |
| **التصميم** | واجهة عربية RTL، ألوان primary (تركواز)، بطاقات، أزرار، شريط سفلي للموبايل فقط. |
| **التواصل مع API** | مكتبة `api` في `src/lib/api.js` تدعم GET/POST مع Bearer token، وترجع بيانات تجريبية عند استخدام `demo-token` أو `guest-token` لطلبات GET. |

### 3.2 الصفحات

- **Login:** نموذج بريد وكلمة مرور، زر "الدخول كزائر"، ومعالجة فشل الشبكة (دخول تجريبي).
- **Home:** رسالة ترحيب + ثلاث بطاقات اختصار (فيديوهات، حجز الطلبة، الغياب والدرجات).
- **Videos:** جلب قائمة الفيديوهات من `/api/videos` وعرض العنوان والوصف ورابط "مشاهدة".
- **Booking:** جلب الحجوزات من `/api/booking` وعرض الاسم، التاريخ، الوقت، الحالة، الملاحظة.
- **Grades:** جلب السجلات من `/api/grades` مع تمييز "انتباه" عندما عدد الغياب > 3.
- **Dashboard:** تجميع إحصائيات من ثلاثة endpoints وروابط سريعة للأقسام.

### 3.3 المكونات المشتركة

- **Layout:** هيكل موحد (Header + محتوى + BottomNav).
- **Header:** عنوان ديناميكي حسب المسار + أزرار "لوحة التحكم" و"خروج".
- **BottomNav:** يظهر على الشاشات الصغيرة فقط (md:hidden) مع أربعة روابط.
- **Card:** مكون بطاقة واحد يُستخدم في الرئيسية وجميع القوائم.
- **ProtectedRoute:** عرض "جاري التحميل" ثم إما المحتوى أو إعادة التوجيه لـ `/login`.

### 3.4 الخادم (Backend)

- **تسجيل الدخول:** `POST /api/auth/login` مع التحقق من البريد وكلمة المرور (bcrypt) وإرجاع JWT.
- **فيديوهات:** `GET /api/videos` و `POST /api/videos` (يتطلب JWT).
- **حجوزات:** `GET /api/booking` و `POST /api/booking` (يتطلب JWT).
- **الغياب والدرجات:** `GET /api/grades` و `POST /api/grades` (يتطلب JWT).
- **البيانات:** تحميل/حفظ من وإلى `server/data.json` مع بيانات افتراضية عند أول تشغيل (مستخدمان: teacher و assistant بكلمة المرور 123456).

---

## 4. التشغيل والبناء (ما هو موثّق في README)

- **تشغيل بدون Backend:** `npm install` ثم `npm run dev` — الدخول التجريبي بأي بريد + 123456 أو "الدخول كزائر".
- **تشغيل مع Backend:** تشغيل `server` (مثلاً `npm run dev` من مجلد `server`) ثم تشغيل الواجهة من الجذر مع proxy لـ `/api`.
- **بناء أندرويد:** تثبيت Capacitor، `npm run build` ثم `npx cap copy` و `npx cap open android` لفتح المشروع في Android Studio وبناء APK.

---

## 5. ملخص الملفات التي تم إنشاؤها/تعديلها (حسب التحليل الحالي)

| النوع | الملفات |
|-------|---------|
| إعدادات المشروع | `package.json`, `vite.config.js`, `tailwind.config.js`, `postcss.config.js`, `capacitor.config.json`, `.gitignore`, `.npmrc` |
| HTML و CSS | `index.html`, `src/index.css` |
| نقطة الدخول وتطبيق React | `src/main.jsx`, `src/App.jsx` |
| السياق والمصادقة | `src/context/AuthContext.jsx` |
| API العميل | `src/lib/api.js` |
| المكونات | `Layout.jsx`, `Header.jsx`, `BottomNav.jsx`, `Card.jsx`, `ProtectedRoute.jsx` |
| الصفحات | `Login.jsx`, `Home.jsx`, `Videos.jsx`, `Booking.jsx`, `Grades.jsx`, `Dashboard.jsx` |
| الخادم | `server/index.js`, `server/package.json` |
| التوثيق | `README.md`, هذا الملف `تقرير-شامل-تطبيق-المدرس.md` |

---

## 6. الخلاصة

تم بناء تطبيق ويب/موبايل كامل للمدرس يشمل:

1. **تسجيل دخول** (حقيقي، زائر، وتجريبي عند غياب السيرفر).
2. **صفحة رئيسية** مع اختصارات للأقسام.
3. **فيديوهات المدرس** (عرض من API مع إمكانية إضافة لاحقة من السيرفر).
4. **حجوزات الطلبة** (عرض القائمة من API).
5. **الغياب والدرجات** (عرض مع تمييز حالات الانتباه).
6. **لوحة تحكم** بإحصائيات وروابط.
7. **Backend Express** مع JWT وتخزين في `data.json`.
8. **واجهة عربية RTL** مع Tailwind وتجربة مناسبة للموبايل وشريط تنقل سفلي.
9. **إعدادات Capacitor** جاهزة لبناء تطبيق أندرويد.

هذا التقرير يعكس **حالة المشروع الحالية** كما هي في الكود. إذا رغبت لاحقاً بتتبع التغييرات تاريخياً، يُفضّل تهيئة مستودع Git وتسجيل الـ commits مع رسائل واضحة.
